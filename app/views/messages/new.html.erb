<div class="container">
  <h1>This is a new message page</h1>
  <%= simple_form_for([@run, @message]) do |m| %>
    <%= m.input :content %>
    <%= m.submit "Submit", class: "btn btn-default" %>
  <% end %>
</div>
