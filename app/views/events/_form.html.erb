<%= simple_form_for(@event) do |e| %>
  <%= e.input :date %>
  <%= e.input :time %>
  <%= e.input :location, priority: ["EN"], input_html: {id: "locationInput"}, placeholder: "Enter a location"  %>
  <%= e.input :run_distance, placeholder: "Kilometre(s)" %>
  <%= e.input :description, placeholder: "e.g. A casual Saturday run along with local bar exploration afterwards" %>
  <%= e.input :surface, collection: [ "Grass", "Earth", "Woodland Trails", "Synthetic Track", "Sand", "Asphalt", "Treadmill", "Concrete", "Snow"], prompt: "Select"%>
  <%= e.input :price, prompt: "£" %>

  <%= e.submit "Submit", class: "btn btn-default" %>
<% end %>

<!-- JS for sweet alert -->
<!-- <script src="https://cdnjs.cloudflare.com/ajax/libs/limonte-sweetalert2/7.0.3/sweetalert2.all.min.js" integrity="sha256-Cc+yDzt4WAIOQNyXND0m3zCkxy8mSRJXLGy+5oGaHHM=" crossorigin="anonymous"></script>
<script type="text/javascript" src="app/assets/javascripts/sweetalert2.js"></script> -->

<!-- JS for google map -->
<%= content_for(:after_js) do %>

  <script>
    var runLocation = document.getElementById("locationInput");
    if (runLocation) {
      var autocomplete = new google.maps.places.Autocomplete(runLocation, { types: ["geocode"] });
      google.maps.event.addDomListener(runLocation, "keydown", function(e) {
        if (e.keyCode == 13) {
          e.preventDefault(); // Do not submit the form on Enter.
        }
      });
    }
  </script>
<% end %>
